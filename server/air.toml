# Config for air (live reload tool)
[build]
  cmd = "go build -o ./tmp/main"
  bin = "tmp/main"
  include_ext = ["go"]
  exclude_dir = ["tmp", "vendor", "node_modules"]
  delay = 1000 # ms

[log]
  level = "debug"

